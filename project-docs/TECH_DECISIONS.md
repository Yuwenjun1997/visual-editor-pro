# 技术决策

## 2026-09-02：首发采用页面版本表

选择 `page_revisions` 保存不可变线上版本，`pages` 继续作为草稿和索引表。这样发布、修改草稿和回滚可以分离，兼容当前 schema 存储方式。

## 2026-09-02：公开页采用受限 RPC

不向匿名客户端开放业务表读取。公开页使用白名单 RPC，避免 RLS 绕过、字段过度暴露和数据源配置被任意利用。

## 2026-09-02：首发暂不做多租户组织

继续以 `user_id` 作为隔离边界，暂不引入 workspace、成员邀请和计费模型。
