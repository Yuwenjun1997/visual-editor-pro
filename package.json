{
  "name": "visual-editor-pro",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rimraf node_modules pnpm-lock.yaml"
  },
  "devDependencies": {
    "@vue/tsconfig": "^0.7.0",
    "prettier": "^3.0.0",
    "rimraf": "^6.0.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}